# Sources / Verification Notes

Primary/current references checked during 2026 refresh:

- nicocha30/ligolo-ng — TUN-based pivoting; current releases include 0.9.x, with 0.8+ Web UI/API/daemon/auto-bind/autoroute improvements.
- jpillora/chisel — TCP/UDP tunnel over HTTP protected with SSH.
- go-gost/gost v3 — current GOST project with TCP/UDP, SOCKS, HTTP2/3, QUIC, WebSocket, SSH, TUN/TAP and forwarding chains.
- Microsoft Dev Tunnels documentation — legitimate local-service exposure/reachability primitive.

Black-cat Dev-Tunnels/C2 material was split by responsibility: reachability stays here; full C2 belongs to `/post`.
